# 情景模块与论文 case 架构

## 当前稳定内核

模型是 31 省、337 城市年度网络代理、8760 小时连续 LP。2025 为边界状态；
2030→2040→2050→2060 逐年独立求解并传递已验收容量 cohort。城市层用于年度能量分配和
省内网络代理，逐时功率平衡仍在省级闭合；278 节点 Natural Earth 输入仅保留为复现资料。

## 当前唯一 case 矩阵

| 情景 | 包含内容 | 用途 |
|---|---|---|
| `base` | 常规风电、海上风电、集中式/分布式光伏、既有 marine-grid 波浪能，以及水电、火电/核电 RUC、储能、输电、337 城市年度网络、碳/CCS/DAC、备用、5% 容量裕度和 3.5 s 惯量 | 唯一供给侧 Base |
| `flexible_load_comfort_v3_v2g_5pct` | 完整继承 Base，再加入 V3 冷热舒适包络/日内热状态、因果 EV V1G 队列和 5% 日内因果 V2G | 唯一需求柔性敏感性 |

波浪能不另建空间网格：它复用既有海洋 `grid_uid` 及 `city_337` 路由；容量信用为零、
不提供备用或惯量，并按实际发电增加 5% 备用需求。运行两个情景都必须提供
`CISPO_WAVE_ROOT`。

历史 V1、state-V2、第一代 V2G、波浪单模块和旧组合配置均已删除。它们的代码/输出/ Git
历史是可追溯证据，不再是可运行或可比较的 case。今后任何灵活性参数或约束完善只修改
`flexible_load_comfort_v3_v2g_5pct`，不得重新引入并行柔性 formulation。

波浪成本与 2060 profile、热工参数、车辆可用性和 V2G 响应成本仍需要敏感性和校准；
这限制的是结论解释，不改变当前 case 架构。情景对比只接受同一边界的 8760h 解；短时
根仅证明实现、QC 和求解性。
