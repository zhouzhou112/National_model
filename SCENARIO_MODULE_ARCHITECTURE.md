# 情景模块与论文 case 架构

## 稳定内核

`base` 是唯一论文基准：31 省、0.25° 风光选址、站点水电、连续 RUC、储能、省际网络、337 个市级年度负荷中心代理、碳与 CCS/DAC，按 2030→2040→2050→2060 顺序独立求解。该空间层不是 337 城市逐时节点调度；省级逐时调度结果按年度需求权重分配到城市代理，并通过年度省内输电容量代理闭合。原 278 节点 Natural Earth 层仅保留用于 CISPO 复现/敏感性。关闭扩展模块时不增加变量或约束。

## 已实现可选模块

| 情景 | 状态 | 新增变量 | 主要硬约束 | 关键输出 |
|---|---|---|---|---|
| `flexible_load_v1` | 24h `OPTIMAL/QC PASS` | 冷热/EV 上下调 | 逐日能量守恒、逐时上下界、有效负荷非负 | `flexible_load_dispatch.npz`、省级年表、QC |
| `flexible_load_state_v2` | 本地 24h `OPTIMAL/QC PASS` | V1 上下调 + 冷热等效库存 + EV 待充队列 | 因果日内状态转移、日末归零、热损耗核算、EV 日能量守恒 | V1 输出 + 冷热 state、EV backlog、状态 QC |
| `flexible_load_v2g_v1` | 24h `OPTIMAL/QC PASS` | V1 + V2G 充放电/SOC | 逐日循环 SOC、效率、功率/能量界、退化成本 | V1 输出 + V2G SOC/损耗/吞吐 |

这三个模块当前用于结构验证和敏感性设计。V2 保留 `Power_curve_V2` 四分量负荷闭合：冷热源于 BAIT/HDD/CDD，未来 EV 基线源于车队规模、月度每车日用电和 `ev_hour_weight`。后者只是无序充电形状，不是车辆接入率；因此 V2 用待充队列表达单向有序充电，并明确禁止与 V2G 同时启用。由于缺少省级逐时车辆接入率、驾驶耗能、出发 SOC、车辆电池容量和建筑热惯性标定，任何柔性情景都不能直接称为“现实校准中心情景”。

静态 8760h 规模估计分别为：Base 40.91M 变量/67.60M 约束，柔性 V1 42.54M/67.91M，状态 V2 43.36M/68.72M，V2G V1 43.36M/68.18M。状态 V2 的当前估计为 524.28M 非零元和 36.84 GiB 静态模型内存；屏障分解仍可能显著高于静态估计，因此云节点内存门槛不因这一估计而下调。

## 互补性模块的冻结顺序

互补情景尚未进入可运行目录。实现顺序必须是：

1. 先冻结经济最优成本 `C*` 和允许松弛比例 `epsilon`；
2. 定义同一套可加总、无尺度偏置的互补指标；
3. 分别实现点内、产地省内和全国协同层级；
4. 用线性辅助变量保持 LP，只有指标无法线性化时才进入 QP/QCP；
5. 24h/168h 验证成本上界、指标闭合和空间聚合无重复计数；
6. 最后才建立 `integrated_realistic`，避免把未校准模块一起开启后无法解释机制。

## 建议 case 矩阵

第一层是 Base 参数敏感性：WACC、核电寿命、燃料价格、技术 CAPEX、容量裕度、惯量阈值和碳约束。第二层是单模块机制：冷热、EV V1G、V2G、点内/省内/全国互补。第三层是交互：柔性负荷是否替代储能/输电，是否放大或削弱空间互补价值。第四层才是经校准的集成路径。

每个全量 case 至少保留成本分解、容量、发电、弃电、小时供需、省际流、省内网络、储能、水电、碳/CCS、边际价格、负荷分量、柔性吞吐、峰值和 planning state。情景对比只接受 8760h；短时结果仅证明代码和约束可运行。
