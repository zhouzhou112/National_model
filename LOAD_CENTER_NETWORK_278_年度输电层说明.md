# 278中心Natural Earth年度输电分配层

## 1. 研究边界

本版本采用278个Natural Earth城市斑块几何中心作为正式负荷中心。中心只在省内连接；省际输电仍由原有省级8760小时线路变量决定。新增层只做选定时段的能量分配，不建立负荷中心小时潮流。

2025是边界状态，2030是第一个规划年。744小时和4344小时运行仅用于代码与求解器测试，只有8760小时结果可以作科研解释。

## 2. 数据来源与生成

执行：

```powershell
python scripts/build_natural_earth_278_network.py
```

正式输入位于：

```text
data/load_center_network/natural_earth_278/
```

核心文件：

| 文件 | 内容 |
|---|---|
| `load_centers.csv` | 278个中心坐标、所属省份和年度需求份额 |
| `vre_routes.csv` | 风光格点到变电站及负荷中心路线 |
| `hydro_routes.csv` | 水电站到变电站及负荷中心路线 |
| `initial_spur_capacity_2025.csv` | 论文路线下的2025 spur容量代理 |
| `substation_initial_capacity_2025.csv` | 论文路线下的2025 trunk/VRE接口代理 |
| `intra_edges.csv` | 省内500 kV候选边、成本和2025容量代理 |
| `initial_capacity_2025_audit.csv` | 2025省内容量反推审计 |
| `manifest.json` | 输入输出校验和与数量统计 |

中心来自Natural Earth 1:50m城市斑块，西藏补充1:10m斑块。1 km电力消费栅格只用于计算省内年度需求份额，不移动中心几何坐标，也不构造中心小时负荷。

风光格点在同省所有合格变电站中最小化：

```math
d^{spur}_{grid,sub}+d^{trunk}_{sub,lc}
```

水电站使用相同目标补齐路线。DPV位于负荷中心侧，spur/trunk容量和成本为0。

## 3. 年度能量归属

风光和水电仍在省级逐小时调度。新增年度变量把实际发电量归属到其空间连接的负荷中心，并满足：

```math
0\le E_{lc,k}\le\sum_{i\rightarrow lc,k}P_i\sum_tcf_{i,t}
```

```math
\sum_{lc\in LC_p}E_{lc,k}=\sum_tG_{p,k,t}
```

因此中心注入量不是理论发电量，省级弃电仍由原小时调度决定。由于省级风光出力没有点位小时决策，弃电在同省同技术各中心间由年度优化分配；这是当前空间归属层的线性松弛。

火电、核电、生物质、储能充放电、DAC和省际受入电量没有完整中心级空间调度，按固定年度需求份额分配。为避免把同一组小时变量重复写入每个中心，模型先建立31个省级年度汇总变量，再连接278中心。

## 4. 中心能量平衡

对每个中心：

```math
J_{lc}+\sum F^{in}_{lc}
=D^{eff}_{lc}+\sum F^{out}_{lc}+X_{lc}
```

`D_eff`包括用户负荷、分配的储能充电和DAC电耗。`X`是分配给中心的省际送端电量，并满足：

```math
\sum_{lc\in LC_p}X_{lc}
=\sum_t\sum_{e\in OUT(p)}F^{send}_{e,t}
```

省际受端电量已经扣除省际线路损耗，再按需求份额进入各中心。对一个省的全部中心平衡求和，可恢复原省级小时平衡的年度总量。

本地使用没有线路成本；省内流动有正的容量成本和极小流量正则成本。因此模型会优先本地消纳，并把固定省际外送量优先分配给省内转运机会成本较高的远端剩余电量。

第一版没有把省际线路物理端点映射为中心级网关。因此 `X` 是省级外送电量的空间归属变量，不包含负荷中心到省界/换流站的额外线路成本。这一设定正是“远离省内大负荷中心的剩余电量优先归为外送”的简化机制，但不能解释为真实省际送端位置。

## 5. 省内拓扑与容量

每省建立最小生成树，并为每个中心增加3个最近邻，保证连通且避免省内完全图。当前共517条无向边，只允许同省中心相连。

```math
F_e^{\to}+F_e^{\leftarrow}
\le H\rho^{design}(K_e^{2025}+K_e^{new})
```

默认 `rho_design=0.5`。这是年度GWh转换为GW所必需的软假设，必须至少测试低、中、高三组利用率。

2025初始容量使用同时铭牌压力代理：风光、水电按空间位置注入，火电、核电和生物质按负荷份额分配，再以最小距离省内流动反推容量。该值只用于避免把全部既有网架重复计为2030新增投资，不是线路观测额定容量。

## 6. 500 kV成本

采用EES Table S20和Figure S44的 `AC_500kV` 参数：

- 变电站：159 yuan/kW；
- 架空线：2640 thousand yuan/km；
- 线路参考传输能力由距离曲线确定；
- 线路寿命：50年。

单位容量投资：

```math
c_e=159+\frac{2640d_e}{1000\bar K_e}
```

年度投资成本：

```math
C_e=CRF_{50}\,c_eK_e^{new}
```

这里的500 kV对应工程设备中常见的550 kV最高工作电压等级，但模型电压名称保持与EES一致的 `AC_500kV`。

## 7. QC和限制

- 278个中心、31省，省级需求份额最大误差不超过 `1e-9`。
- 16,609个风光格点和2,030个水电站均有唯一空间路线。
- 517条候选边均位于同一省份，初始容量非负，成本为正。
- 西藏和内蒙古各有1条候选边超过EES AC500的1000 km来源范围，标记为软例外。
- 中心线路为大圆距离代理，不是实际工程路径。
- 水库水电的小时库容约束仍在省级执行；年度中心归属只受各中心水库装机乘时段小时数约束，因此省内水库发电空间分配属于线性松弛。
- 第一版省内损耗设为0。若加入损耗，必须把年度损耗作为额外用电反馈到省级小时平衡，不能只在中心层扣减。
- 2025初始容量与50%利用率均需敏感性分析。

## 8. 验证命令

```powershell
python scripts/build_natural_earth_278_network.py
python -m unittest discover -s tests -v
python scripts/run_cispo_2030_full_year.py --horizon one_month --build-only --skip-full-max-cf
```

当前24小时开发烟雾求解已达到最优解，用于验证年度层可行性；该结果不具有情景解释意义。
